! Auto-generated Cisco IOS configuration for {{ hostname }}
! Do NOT edit manually.

enable
configure terminal

hostname {{ hostname }}

! Loopback interface
interface Loopback0
 ip address {{ loopback_ip }} {{ loopback_mask }}
!

! Point-to-point interfaces
{% for intf in interfaces %}
interface {{ intf.name }}
 description {{ intf.description }}
 ip address {{ intf.ip }} {{ intf.mask }}
 no shutdown
!
{% endfor %}

! OSPF configuration
router ospf 1
 router-id {{ router_id }}
{% for intf in interfaces %}
 network {{ intf.network }} {{ intf.wildcard }} area 0
{% endfor %}
 network {{ loopback_ip }} 0.0.0.0 area 0
!
end
